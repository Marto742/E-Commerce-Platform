{
  "name": "@repo/email",
  "version": "0.1.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.tsx"
  },
  "scripts": {
    "build": "tsc",
    "dev": "node -e \"console.log('[@repo/email] dev server not configured yet')\"",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:^",
    "@repo/tsconfig": "workspace:^",
    "eslint": "^8.57.1",
    "typescript": "^5.7.2"
  }
}
