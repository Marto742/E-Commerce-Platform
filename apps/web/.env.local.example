# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API
NEXT_PUBLIC_API_URL=http://localhost:4000/v1

# Stripe (publishable key only — secret key lives on the API)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_
